# Python Geospatial Workshop Requirements
# PyDataVT2025 - Interactive Vermont Geology Explorer
#
# Installation:
#   pip install -r requirements.txt
#
# Note: Some packages (like GDAL/Fiona) may require system-level dependencies.
# Consider using conda for easier geospatial package management:
#   conda install -c conda-forge geopandas folium ipywidgets

# Core geospatial libraries
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0

# Visualization and mapping
folium>=0.15.0
matplotlib>=3.8.0

# Data manipulation
pandas>=2.1.0

# HTTP requests for API calls
requests>=2.31.0

# Interactive widgets for Jupyter
ipywidgets>=8.1.0

# Jupyter notebook support
jupyter>=1.0.0
notebook>=7.0.0

# Optional but recommended
# contextily>=1.4.0  # For adding basemaps (if needed in future)
# rasterio>=1.3.0    # For raster data processing (if needed in future)
